# Gemma 4 API configuration.
# Leave unset to run KIBO fully on simulated responses (default, no key needed).
# Set both to route real requests through services/gemma.ts.

# NEXT_PUBLIC_GEMMA_API_URL=https://your-gemma-endpoint.example.com
# GEMMA_API_KEY=your-api-key
